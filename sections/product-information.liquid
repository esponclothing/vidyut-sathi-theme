{%- liquid
  assign current_variant = product.selected_or_first_available_variant
  assign product_form_id = 'product-form-' | append: section.id
-%}

{%- style -%}
  {{ section.settings.heading_font | font_face: font_display: 'swap' }}
  {{ section.settings.body_font | font_face: font_display: 'swap' }}

  .by-product-{{ section.id }} {
    --bg-color: {{ section.settings.bg_color }};
    --text-color: {{ section.settings.text_color }};
    --text-light: {{ section.settings.text_color | color_modify: 'alpha', 0.6 }};
    --primary-btn: {{ section.settings.primary_button_color }};
    --primary-btn-text: {{ section.settings.primary_button_text }};
    --border-color: {{ section.settings.border_color }};
    --offer-border: {{ section.settings.offer_accent_color }};
    --offer-bg: {{ section.settings.offer_bg_color }};
    
    --font-heading: {{ section.settings.heading_font.family }}, {{ section.settings.heading_font.fallback_families }};
    --font-body: {{ section.settings.body_font.family }}, {{ section.settings.body_font.fallback_families }};

    background-color: var(--bg-color);
    color: var(--text-color);
    font-family: var(--font-body);
    max-width: 768px;
    margin: 0 auto;
    padding-bottom: 110px;
  }

  .by-product-{{ section.id }} * { box-sizing: border-box; }

  /* --- MEDIA GALLERY --- */
  .by-media-gallery { width: 100%; background: #f7f7f7; }
  .by-main-image { width: 100%; aspect-ratio: 4/5; overflow: hidden; position: relative; }
  .by-main-image img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .by-thumbnails {
    display: flex; gap: 10px; padding: 10px 16px;
    overflow-x: auto; scrollbar-width: none; background: var(--bg-color);
  }
  .by-thumbnails::-webkit-scrollbar { display: none; }
  .by-thumb {
    width: 65px; height: 85px; border-radius: 6px;
    border: 1px solid var(--border-color); overflow: hidden;
    flex-shrink: 0; cursor: pointer;
  }
  .by-thumb.active { border: 2px solid var(--text-color); }
  .by-thumb img { width: 100%; height: 100%; object-fit: cover; }

  /* --- CONTENT AREA & TITLE --- */
  .by-content-wrapper { padding: 20px 16px; }

  .by-title-row {
    display: flex; justify-content: space-between; align-items: flex-start;
    margin-bottom: 12px;
  }
  .by-title-row h1 {
    font-family: var(--font-heading); font-size: 1.4rem; margin: 0;
    font-weight: 700; color: var(--text-color); line-height: 1.3;
  }
  .by-wishlist-btn { background: none; border: none; cursor: pointer; padding: 4px; }
  
  /* --- MAIN PRICING --- */
  .by-price-wrapper { display: flex; align-items: center; gap: 12px; margin-bottom: 24px; }
  .by-current-price { font-size: 1.6rem; font-weight: 800; color: var(--text-color); }
  .by-compare-price { font-size: 1.1rem; color: var(--text-light); text-decoration: line-through; }
  .by-save-badge {
    background: #E8F5E9; color: #2E7D32; font-size: 0.8rem;
    font-weight: 700; padding: 4px 8px; border-radius: 4px; text-transform: uppercase;
  }

  /* --- COUPON BOX --- */
  .by-coupon-box {
    display: flex; justify-content: space-between; align-items: center;
    background: var(--offer-bg); border: 1.5px dashed var(--offer-border);
    padding: 16px; border-radius: 8px; margin-bottom: 28px;
  }
  .by-coupon-info { display: flex; flex-direction: column; gap: 4px; }
  .by-coupon-info strong { font-size: 0.95rem; color: var(--text-color); }
  .by-coupon-info span { font-size: 0.8rem; color: var(--text-light); }
  .by-coupon-code {
    background: #fff; border: 1px solid var(--offer-border);
    padding: 6px 12px; border-radius: 6px; font-weight: 700;
    color: var(--text-color); font-size: 0.9rem; letter-spacing: 0.5px;
  }

  /* --- COMBO PACK SELECTION --- */
  .by-section-header {
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: 16px; margin-top: 24px;
  }
  .by-section-title { font-weight: 800; font-size: 1.15rem; }
  
  .by-install-btn {
    display: inline-flex; align-items: center; gap: 6px;
    font-size: 0.8rem; font-weight: 600; color: var(--text-color);
    background: #fff; padding: 6px 14px; border-radius: 20px;
    border: 1px solid var(--border-color); text-decoration: none;
    box-shadow: 0 2px 5px rgba(0,0,0,0.05); transition: all 0.2s ease;
  }
  
  .by-combo-grid { display: flex; flex-direction: column; gap: 14px; }
  .by-combo-card {
    display: flex; justify-content: space-between; align-items: center; gap: 16px;
    border: 1.5px solid var(--border-color); padding: 18px 16px; 
    border-radius: 10px; cursor: pointer; position: relative;
    background: #fff; transition: all 0.3s ease;
  }
  .by-combo-card.active {
    border-color: var(--primary-btn); background: #FFFAEB;
    box-shadow: 0 4px 15px rgba(0,0,0,0.05);
  }
  
  .by-combo-left { display: flex; align-items: center; gap: 12px; }
  .by-combo-radio {
    width: 24px; height: 24px; border-radius: 50%;
    border: 2px solid #ccc; position: relative; flex-shrink: 0;
  }
  .by-combo-card.active .by-combo-radio { border-color: var(--primary-btn); }
  .by-combo-card.active .by-combo-radio::after {
    content: ''; position: absolute; top: 4px; left: 4px; right: 4px; bottom: 4px;
    background: var(--primary-btn); border-radius: 50%;
  }
  
  .by-combo-info { display: flex; flex-direction: column; gap: 3px; }
  .by-combo-title { font-weight: 800; font-size: 1.05rem; color: var(--text-color); }
  .by-combo-desc { font-size: 0.85rem; color: var(--text-light); }

  .by-combo-right { display: flex; flex-direction: column; align-items: flex-end; text-align: right; }
  .by-combo-price { font-weight: 800; font-size: 1.15rem; color: var(--text-color); }
  .by-combo-compare { font-size: 0.85rem; color: var(--text-light); text-decoration: line-through; margin-bottom: 2px; }
  
  .by-combo-badge {
    position: absolute; left: 16px; top: -10px; 
    background: var(--primary-btn); color: var(--primary-btn-text);
    font-size: 0.75rem; font-weight: 800; padding: 4px 12px;
    border-radius: 20px; text-transform: uppercase;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }

  /* --- DELIVERY & ACCORDIONS --- */
  .by-delivery-wrapper { margin-top: 36px; }
  .by-delivery-input-group { display: flex; border: 1px solid var(--border-color); border-radius: 8px; overflow: hidden; }
  .by-delivery-input-group input { flex: 1; padding: 16px; border: none; outline: none; font-family: inherit; }
  .by-delivery-input-group button { background: var(--text-color); color: var(--bg-color); border: none; padding: 0 24px; font-weight: bold; cursor: pointer; }

  .by-details-container { margin-top: 36px; }
  .by-accordion { border-bottom: 1px solid var(--border-color); }
  .by-accordion:first-of-type { border-top: 1px solid var(--border-color); }
  .by-accordion summary { padding: 18px 0; font-weight: 700; cursor: pointer; list-style: none; display: flex; justify-content: space-between; }
  .by-accordion summary::-webkit-details-marker { display: none; }
  .by-accordion summary::after { content: '+'; font-size: 1.4rem; transition: transform 0.3s; }
  .by-accordion[open] summary::after { transform: rotate(45deg); }
  .by-accordion-content { padding-bottom: 18px; font-size: 0.95rem; color: var(--text-light); line-height: 1.6; }

  /* --- STICKY BOTTOM ACTIONS --- */
  .by-sticky-bar {
    position: fixed; bottom: 0; left: 0; right: 0;
    background: var(--bg-color); padding: 16px;
    box-shadow: 0 -10px 30px rgba(0,0,0,0.08); display: flex; z-index: 999;
    max-width: 768px; margin: 0 auto; border-top: 1px solid var(--border-color);
  }
  .by-btn-add {
    width: 100%; padding: 18px; border: none; border-radius: 8px;
    font-weight: 800; font-size: 1.15rem; cursor: pointer;
    text-transform: uppercase; letter-spacing: 1px;
    background: var(--primary-btn); color: var(--primary-btn-text);
    box-shadow: 0 4px 15px rgba(0,0,0,0.1); transition: transform 0.2s ease;
    display: flex; justify-content: center; align-items: center; gap: 8px;
  }
  .by-btn-add:active { transform: scale(0.98); }
  .by-btn-add.loading { opacity: 0.8; pointer-events: none; }

  @media (min-width: 769px) {
    .by-sticky-bar { position: sticky; bottom: 20px; border-radius: 12px; border: 1px solid var(--border-color); }
    .by-product-{{ section.id }} { padding-bottom: 20px; }
  }
{%- endstyle -%}

<div class="by-product-{{ section.id }}">
  
  <div class="by-media-gallery">
    <div class="by-main-image">
      {%- if product.featured_media -%}
        <img src="{{ product.featured_media | img_url: '800x' }}" alt="{{ product.title | escape }}">
      {%- else -%}
        <div style="width: 100%; height: 100%; background: #e0e0e0; display:flex; align-items:center; justify-content:center; color: #666;">Product Image</div>
      {%- endif -%}
    </div>
    <div class="by-thumbnails">
      {%- for media in product.media -%}
        <div class="by-thumb {% if forloop.first %}active{% endif %}">
          <img src="{{ media | img_url: '150x' }}" alt="{{ media.alt | escape }}">
        </div>
      {%- endfor -%}
    </div>
  </div>

  <div class="by-content-wrapper">
    
    {%- form 'product', product, id: product_form_id, class: 'form', novalidate: 'novalidate', data-type: 'add-to-cart-form' -%}
      
      <!-- Hidden Input for Cart: This is updated by JavaScript when a combo is clicked -->
      <input type="hidden" name="id" id="SelectedVariantID-{{ section.id }}" value="{{ current_variant.id }}">
      <input type="hidden" name="quantity" value="1"> <!-- Quantity is 1 because the variant ITSELF is the multi-pack -->

      <div class="by-title-row">
        <h1>{{ product.title | default: 'Premium Product Name' }}</h1>
      </div>

      <div class="by-price-wrapper">
        <span class="by-current-price">{{ product.price | default: 199900 | money }}</span>
        {%- if product.compare_at_price > product.price -%}
          <span class="by-compare-price">{{ product.compare_at_price | money }}</span>
          <span class="by-save-badge">Save {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}%</span>
        {%- endif -%}
      </div>

      {%- if section.settings.coupon_enable -%}
        <div class="by-coupon-box">
          <div class="by-coupon-info">
            <strong>{{ section.settings.coupon_title }}</strong>
            <span>{{ section.settings.coupon_subtitle }}</span>
          </div>
          <div class="by-coupon-code">{{ section.settings.coupon_code }}</div>
        </div>
      {%- endif -%}

      <div class="by-section-header">
        <span class="by-section-title">Select Pack Options</span>
        {%- if section.settings.install_guide_link != blank -%}
          <a href="{{ section.settings.install_guide_link }}" class="by-install-btn" target="_blank">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="16" x2="12" y2="12"></line><line x1="12" y1="8" x2="12.01" y2="8"></line></svg>
            {{ section.settings.install_guide_text }}
          </a>
        {%- endif -%}
      </div>
      
      <div class="by-combo-grid" id="ComboGrid-{{ section.id }}">
        
        <!-- Option 1: Single Pack -->
        {%- if section.settings.pack_1_enable -%}
        {%- assign p1_variant = product.variants | where: 'title', section.settings.pack_1_variant | first -%}
        {%- assign p1_id = p1_variant.id | default: current_variant.id -%}
        
        <label class="by-combo-card active">
          <input type="radio" name="combo_selection" data-variant-id="{{ p1_id }}" checked style="display:none;">
          {%- if section.settings.pack_1_badge != blank -%}
            <span class="by-combo-badge">{{ section.settings.pack_1_badge }}</span>
          {%- endif -%}
          <div class="by-combo-left">
            <div class="by-combo-radio"></div>
            <div class="by-combo-info">
              <span class="by-combo-title">{{ section.settings.pack_1_title }}</span>
              <span class="by-combo-desc">{{ section.settings.pack_1_subtitle }}</span>
            </div>
          </div>
          <div class="by-combo-right">
            {%- if section.settings.pack_1_compare_price != blank -%}
              <span class="by-combo-compare">{{ section.settings.pack_1_compare_price }}</span>
            {%- endif -%}
            {%- if section.settings.pack_1_price != blank -%}
              <span class="by-combo-price">{{ section.settings.pack_1_price }}</span>
            {%- endif -%}
          </div>
        </label>
        {%- endif -%}

        <!-- Option 2: Trio Pack -->
        {%- if section.settings.pack_2_enable -%}
        {%- assign p2_variant = product.variants | where: 'title', section.settings.pack_2_variant | first -%}
        {%- assign p2_id = p2_variant.id | default: current_variant.id -%}

        <label class="by-combo-card">
          <input type="radio" name="combo_selection" data-variant-id="{{ p2_id }}" style="display:none;">
          {%- if section.settings.pack_2_badge != blank -%}
            <span class="by-combo-badge">{{ section.settings.pack_2_badge }}</span>
          {%- endif -%}
          <div class="by-combo-left">
            <div class="by-combo-radio"></div>
            <div class="by-combo-info">
              <span class="by-combo-title">{{ section.settings.pack_2_title }}</span>
              <span class="by-combo-desc">{{ section.settings.pack_2_subtitle }}</span>
            </div>
          </div>
          <div class="by-combo-right">
            {%- if section.settings.pack_2_compare_price != blank -%}
              <span class="by-combo-compare">{{ section.settings.pack_2_compare_price }}</span>
            {%- endif -%}
            {%- if section.settings.pack_2_price != blank -%}
              <span class="by-combo-price">{{ section.settings.pack_2_price }}</span>
            {%- endif -%}
          </div>
        </label>
        {%- endif -%}

        <!-- Option 3: Pack of 5 -->
        {%- if section.settings.pack_3_enable -%}
        {%- assign p3_variant = product.variants | where: 'title', section.settings.pack_3_variant | first -%}
        {%- assign p3_id = p3_variant.id | default: current_variant.id -%}

        <label class="by-combo-card">
          <input type="radio" name="combo_selection" data-variant-id="{{ p3_id }}" style="display:none;">
          {%- if section.settings.pack_3_badge != blank -%}
            <span class="by-combo-badge">{{ section.settings.pack_3_badge }}</span>
          {%- endif -%}
          <div class="by-combo-left">
            <div class="by-combo-radio"></div>
            <div class="by-combo-info">
              <span class="by-combo-title">{{ section.settings.pack_3_title }}</span>
              <span class="by-combo-desc">{{ section.settings.pack_3_subtitle }}</span>
            </div>
          </div>
          <div class="by-combo-right">
            {%- if section.settings.pack_3_compare_price != blank -%}
              <span class="by-combo-compare">{{ section.settings.pack_3_compare_price }}</span>
            {%- endif -%}
            {%- if section.settings.pack_3_price != blank -%}
              <span class="by-combo-price">{{ section.settings.pack_3_price }}</span>
            {%- endif -%}
          </div>
        </label>
        {%- endif -%}

      </div>

      <div class="by-details-container">
        <details class="by-accordion" open>
          <summary>Product Description</summary>
          <div class="by-accordion-content">{{ product.description | default: 'Experience premium quality.' }}</div>
        </details>
        <details class="by-accordion">
          <summary>Returns & Exchange</summary>
          <div class="by-accordion-content">Easy 15 days return and exchange policy available.</div>
        </details>
      </div>

      <!-- Submit Button -->
      <div class="by-sticky-bar">
        <button class="by-btn-add" type="submit" name="add" id="SubmitBtn-{{ section.id }}">
          <span>Add to Cart</span> 
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="margin-left: 6px;"><path d="M5 12h14"></path><path d="M12 5l7 7-7 7"></path></svg>
        </button>
      </div>

    {%- endform -%}

  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const grid = document.getElementById('ComboGrid-{{ section.id }}');
    const mainVariantInput = document.getElementById('SelectedVariantID-{{ section.id }}');
    
    // Ensure default selected variant ID is set on page load
    const defaultRadio = grid ? grid.querySelector('input[type="radio"]:checked') : null;
    if(defaultRadio && mainVariantInput) {
       mainVariantInput.value = defaultRadio.getAttribute('data-variant-id');
    }

    if(grid) {
      const cards = grid.querySelectorAll('.by-combo-card');
      cards.forEach(card => {
        card.addEventListener('click', function() {
          // Visual updates
          cards.forEach(c => c.classList.remove('active'));
          this.classList.add('active');
          
          // Update the Hidden Variant ID that Shopify actually adds to the cart
          const radio = this.querySelector('input[type="radio"]');
          if(radio) {
            radio.checked = true;
            if(mainVariantInput) {
              mainVariantInput.value = radio.getAttribute('data-variant-id');
            }
          }
        });
      });
    }

    // Button loading state
    const form = document.getElementById('{{ product_form_id }}');
    const btn = document.getElementById('SubmitBtn-{{ section.id }}');
    if(form && btn) {
      form.addEventListener('submit', function() {
        btn.classList.add('loading');
        btn.querySelector('span').innerText = 'Adding...';
        setTimeout(() => { btn.classList.remove('loading'); btn.querySelector('span').innerText = 'Add to Cart'; }, 2000);
      });
    }
  });
</script>

{% schema %}
{
  "name": "Combo Product Info",
  "settings": [
    {
      "type": "header",
      "content": "IMPORTANT SETUP"
    },
    {
      "type": "paragraph",
      "content": "To add specific discounted prices to the cart, you must create Variants in your Shopify Admin (e.g. named 'Single', 'Trio', 'Pack of 5') and set their prices there. Type the exact Variant Name into the 'Link to Variant Name' boxes below."
    },
    {
      "type": "header",
      "content": "Color Configuration"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "primary_button_color",
      "label": "Add To Cart / Accent Color",
      "default": "#FBD100"
    },
    {
      "type": "color",
      "id": "primary_button_text",
      "label": "Button Text Color",
      "default": "#000000"
    },
    {
      "type": "header",
      "content": "Combo Pack 1 (Single)"
    },
    {
      "type": "checkbox",
      "id": "pack_1_enable",
      "label": "Enable Pack 1",
      "default": true
    },
    {
      "type": "text",
      "id": "pack_1_variant",
      "label": "Link to Shopify Variant Name",
      "default": "Single",
      "info": "Must exactly match the variant title in Shopify admin."
    },
    {
      "type": "text",
      "id": "pack_1_title",
      "label": "Display Title",
      "default": "Single Pack"
    },
    {
      "type": "text",
      "id": "pack_1_subtitle",
      "label": "Display Subtitle",
      "default": "Standard Price"
    },
    {
      "type": "text",
      "id": "pack_1_price",
      "label": "Display Price",
      "default": "₹999"
    },
    {
      "type": "text",
      "id": "pack_1_compare_price",
      "label": "Display Compare Price",
      "default": "₹1499"
    },
    {
      "type": "text",
      "id": "pack_1_badge",
      "label": "Highlight Badge (Optional)"
    },
    {
      "type": "header",
      "content": "Combo Pack 2 (Trio)"
    },
    {
      "type": "checkbox",
      "id": "pack_2_enable",
      "label": "Enable Pack 2",
      "default": true
    },
    {
      "type": "text",
      "id": "pack_2_variant",
      "label": "Link to Shopify Variant Name",
      "default": "Trio",
      "info": "Must exactly match the variant title in Shopify admin."
    },
    {
      "type": "text",
      "id": "pack_2_title",
      "label": "Display Title",
      "default": "Trio Pack"
    },
    {
      "type": "text",
      "id": "pack_2_subtitle",
      "label": "Display Subtitle",
      "default": "Pack of 3"
    },
    {
      "type": "text",
      "id": "pack_2_price",
      "label": "Display Price",
      "default": "₹2499"
    },
    {
      "type": "text",
      "id": "pack_2_compare_price",
      "label": "Display Compare Price",
      "default": "₹4497"
    },
    {
      "type": "text",
      "id": "pack_2_badge",
      "label": "Highlight Badge (Optional)",
      "default": "Save 44%"
    },
    {
      "type": "header",
      "content": "Combo Pack 3 (Pack of 5)"
    },
    {
      "type": "checkbox",
      "id": "pack_3_enable",
      "label": "Enable Pack 3",
      "default": true
    },
    {
      "type": "text",
      "id": "pack_3_variant",
      "label": "Link to Shopify Variant Name",
      "default": "Pack of 5",
      "info": "Must exactly match the variant title in Shopify admin."
    },
    {
      "type": "text",
      "id": "pack_3_title",
      "label": "Display Title",
      "default": "Pack of 5"
    },
    {
      "type": "text",
      "id": "pack_3_subtitle",
      "label": "Display Subtitle",
      "default": "Best Value"
    },
    {
      "type": "text",
      "id": "pack_3_price",
      "label": "Display Price",
      "default": "₹3999"
    },
    {
      "type": "text",
      "id": "pack_3_compare_price",
      "label": "Display Compare Price",
      "default": "₹7495"
    },
    {
      "type": "text",
      "id": "pack_3_badge",
      "label": "Highlight Badge (Optional)",
      "default": "Save 46%"
    }
  ],
  "presets": [
    {
      "name": "Functional Combo Info"
    }
  ]
}
{% endschema %}